# magic-crm 기반 프론트엔드 6년 차 (시니어) 이력서 & 면접 기술 소개서

본 문서는 **Next.js 15 App Router, tRPC, Zod, Zustand, unstated-next(Container 패턴), FSD 변형 아키텍처, Strict TypeScript** 기반의 엔터프라이즈 B2B SaaS 프로젝트(`magic-crm`) 경험을 바탕으로 작성된 **이력서 자기소개서 및 면접 기술 대응 전략 통합 가이드**입니다.

---

## 🎯 0. 핵심 포지셔닝 전략 (6년 차 시니어 단계)

면접관이 6년 차 프론트엔드 개발자에게 검증하고자 하는 4대 핵심 역량을 중심으로 포지셔닝합니다.

1. **아키텍처 설계 및 팀 협업 표준화 역량** (FSD 변형 레이어 분리, 단방향 의존성, 가이드라인 제공)
2. **복잡한 도메인 & POS 시스템 설정 개발 역량** (POS 장비/단말기/영수증 템플릿 커스텀 엔진, Drag&Drop 단축키 배치, 듀얼 고객 디스플레이 동기화)
3. **대용량 데이터 처리 및 성능 최적화** (DOM 가상화 렌더링, 수만 건 엑셀 스트리밍 가공 유틸리티)
4. **End-to-End 타입 안전성 및 코드 품질 유지 역량** (tRPC + Zod, Zero `any`, Zero Hardcoding/Magic String)

---

## 💡 1. 자기소개 & 프로젝트 엘리베이터 피치 (Elevator Pitch)

### 📌 [형식 1] 1분 자기소개 & 면접 오프닝 피치 (⭐ 추천)

> **면접 시작 시 1분 자기소개** 또는 **이력서 상단(About Me / Summary)**에 배치하여 핵심 강점을 강렬하게 어필합니다.

```markdown
"안녕하세요, 11개 이상의 핵심 도메인(POS 시스템 설정, POS 결제, 재고, 매출, 마케팅, AI 분석 등)이 유기적으로 연결된 엔터프라이즈 B2B SaaS CRM/POS 웹 시스템의 프론트엔드 아키텍처를 구축하고 복잡한 비즈니스 문제를 해결해 온 6년 차 프론트엔드 개발자 [이름]입니다.

특히 POS 시스템 환경 설정(POS 장비/VAN 단말기 연동, Drag & Drop 기반 판매 단축키 키패드 커스터마이징, 영수증 템플릿 제어 엔진, 듀얼 고객 전면 디스플레이 연동)의 전체 프론트엔드 구조를 완벽하게 설계했습니다. Next.js App Router와 tRPC, Zod를 결합해 백엔드부터 프론트엔드까지 단 한 줄의 타입 누수도 없는 완벽한 End-to-End 타입 안전성을 확보하고, 'Zustand + unstated-next(Container)' 기반의 2-Layer 상태 관리 전략으로 실시간 POS 장비 상태 및 다양한 시스템 설정을 안정적으로 제어했습니다."
```

---

### 📌 [형식 2] 서술형 자기소개서 (Cover Letter)

> **서술형 자기소개서**나 **포트폴리오 서문**에 적합한 형태입니다.

```markdown
[프로덕트의 안정성과 팀의 생산성을 동시에 끌어올리는 6년 차 프론트엔드 개발자]

안녕하세요, 6년 차 프론트엔드 개발자 [이름]입니다.
저는 단순히 화면을 구현하는 것에 그치지 않고, 복잡한 비즈니스 요구사항을 유지보수하기 쉬운 아키텍처와 결함 없는 시스템으로 전환하는 데 가치를 둡니다.

통합 B2B SaaS CRM/POS 시스템 프로젝트에서는 Next.js App Router 위에 실무형 FSD(Feature-Sliced Design) 아키텍처를 도입하여 view(순수 UI), container(로컬 비즈니스 로직), components(재사용 UI), types, constants 계층을 명확히 정의했습니다. 이를 통해 관심사를 철저히 분리하고 단방향 의존성을 보장하여, 대규모 협업 환경에서도 오염 없는 확장성을 확보했습니다.

특히 다양한 매장 환경에 맞춰 POS 장비/단말기 연동, Drag & Drop 기반 단축키 키패드 커스터마이징, 영수증 템플릿 실시간 가공 엔진, 듀얼 고객 디스플레이 동기화 등 복잡도가 매우 높은 POS 시스템 설정 모듈 전반을 총괄 개발했습니다.

또한 서비스 품질과 팀 생산성의 핵심은 '엄격한 타입 안전성'에 있다고 믿습니다. 프로젝트 전반에서 any 타입과 매직 스트링, 하드코딩을 엄격히 배제하였고, tRPC와 Zod Schema를 활용해 API 응답 변경이 프론트엔드 빌드 타임(tsc)에 즉시 감지되는 통합 타입 시스템을 갖췄습니다.

글로벌 세션(인증, POS 장비 설정 등)은 Zustand로 경량화하고, 피처 단위 비즈니스 상태는 unstated-next Container로 캡슐화하는 2-Layer 상태 관리 전략을 통해 리렌더링 최적화와 메모리 생명주기 관리까지 고도화했습니다. 앞으로도 탄탄한 설계 역량과 성능 최적화 경험을 토대로 비즈니스 목표 달성에 크게 기여하는 엔지니어로 성장하겠습니다.
```

---

### 📌 [형식 3] 한 줄 헤드라인 & 키워드 뱃지 (Portfolio / LinkedIn)

> **노션, 링크드인, 포트폴리오 메인 소개**용 간결한 포맷입니다.

```markdown
# 🚀 [이름] | Senior Frontend Engineer (6년 차)

> **"FSD 아키텍처와 tRPC E2E Type Safety로 복잡한 B2B CRM/POS 도메인을 단순하고 견고하게 구현하는 개발자"**

- **Framework & Architecture**: React 18, Next.js 15 (App Router), Modified FSD (`view` / `container` / `components` / `types` / `constants`)
- **Type Safety & API**: Strict TypeScript, tRPC (`@trpc/server`, `@trpc/client`), Zod (Zero `any`, Zero Hardcoding)
- **State Management**: 2-Layer Architecture — Zustand (Global Session/Auth) + unstated-next (Feature Container)
- **Performance & Ecosystem**: TanStack Virtual / Table, Radix UI, Storybook, Recharts, DnD Kit, Excel Stream Export
- **Testing & Quality**: Vitest, MSW, Testing Library, `useOverlay` Declarative Modal Policy
```

---

## 🚀 2. 면접에서 반드시 어필해야 할 4대 핵심 기술 포인트

### ① [아키텍처/타입 안전성] FSD 기반 레이어 분리 & tRPC End-to-End 타입 시스템 구축

- **배경 & 문제점**:
  - 대규모 CRM/ERP 프로젝트는 도메인이 커질수록 상태 얽힘 현상이 발생하고, 백엔드 API 명세 변경 시 프론트엔드에서 런타임 에러(`Null/Undefined`)가 빈번하게 발생합니다. 또한 개발자마다 폴더/로직 분리 기준이 달라 기술 부채가 누적됩니다.
- **해결 방법 & 설계 포인트**:
  - **FSD(Feature-Sliced Design) 변형 아키텍처 도입**: 코드를 명확한 책임 단위 (`view`: 순수 UI 렌더링, `container`: 로컬 뷰-컨트롤러/비즈니스 로직, `components`: 재사용 UI 컴포넌트, `types`: TypeScript 인터페이스/Enum, `constants`: 상수값)로 철저히 분리했습니다.
  - **tRPC + Zod 기반 E2E Type Safety**: `@trpc/client`, `@trpc/next`, `@trpc/server`와 Zod 스키마를 결합하여 백엔드 라우터 타입과 프론트엔드 호출부가 100% 실시간 동기화되도록 구축했습니다. API 변경 시 빌드 타임(`tsc`)에 감지됩니다.
  - **Strict Code Quality Rule**: 프로젝트 전반에 `any` 타입 사용과 매직 스트링, 하드코딩을 전면 금지하고 상수로 구조화하여 유지보수성을 극대화했습니다.

---

### ② [상태 관리 & 복잡한 결제/도메인 로직] 2-Layer 상태 관리 전략 및 POS 결제 플로우 제어

- **배경 & 문제점**:
  - 모든 상태를 전역 스토어(Redux, Zustand 등)에 과도하게 밀어 넣으면 스토어가 비대해지고 원치 않는 전역 리렌더링이 유발됩니다. 또한 POS 복합 결제, 쿠폰 마케팅 연동 등 예외처리가 복잡한 비즈니스 로직 제어 시 상태 오염 위험이 큽니다.
- **해결 방법 & 설계 포인트**:
  - **2-Layer 상태 관리 전략**:
    - **전역 공유 데이터 Layer**: 로그인 세션, 매장 설정, POS 장비 연동 등 생명주기가 앱 전체인 데이터는 `Zustand`로 경량화하여 관리.
    - **피처/페이지 단위 로컬 상태 Layer**: `unstated-next` 기반의 Container 패턴(`OtherStoresContainer`, `PosReceiptContainer` 등)을 적용해 화면(`view.tsx`)과 뷰 컨트롤러 비즈니스 로직을 격리하고, 마운트 해제 시 자동 가비지 컬렉션(GC) 유도.
  - **결제 오류 및 선언적 오버레이 정책 고도화**:
    - POS 결제 연동(`EXCHANGE_VOUCHER` 복합 결제 등) 처리 중 발생하는 에러는 치명적이므로, 단순 Toast 통보 방식 대신 사용자 승인을 보장하는 **Alert/Confirm 모달 정책**을 수립.
    - `useOverlay` 커스텀 훅을 설계하여 비동기 결제 승인 흐름과 중첩 모달 상태 제어를 선언적이고 명확하게 처리했습니다.

---

### ③ [대용량 데이터 & 성능 최적화] DOM 가상화(Virtualization)와 수만 건 엑셀 가공/내보내기

- **배경 & 문제점**:
  - 타점 재고 검색(`other-stores`), 일자별/시간대별 대용량 매출/거래 내역 등 수천~수만 건의 그리드 데이터를 렌더링하거나 엑셀로 추출할 때 대량의 DOM 생성으로 인한 브라우저 프리징(Freezing) 및 메모리 병목 현상이 발생합니다.
- **해결 방법 & 설계 포인트**:
  - **DOM 가상화 렌더링**: `@tanstack/react-table`과 `@tanstack/react-virtual`을 결합하여 현재 뷰포트에 보이는 DOM 노드만 동적으로 렌더링함으로써 수만 건 데이터 환경에서도 **60fps의 매끄러운 스크롤 성능**을 달성했습니다.
  - **클라이언트 엑셀 가공 유틸리티 설계**: `xlsx` 및 `xlsx-js-style`을 활용하여 최대 수만 건(`EXCEL_EXPORT_MAX_ROWS`) 데이터 가공 시 브라우저 병목 없이 병합/정렬/셀 스타일링 후 바로 추출 가능한 `fetchAndExportToExcel` 모듈을 설계했습니다.

---

### ④ [POS 시스템 설정 & 하드웨어/디스플레이 제어 엔진] 멀티 디바이스 POS 환경 설정 및 영수증/단축키 커스텀 시스템 개발

- **배경 & 문제점**:
  - 일반 웹 서비스와 달리 B2B POS CRM 시스템은 매장 환경(VAN/PG 단말기 모델, 영수증 프린터 규격/포트, 듀얼 모니터 고객 디스플레이, 매장별 판매 키패드 레이아웃)에 따라 수십 가지의 POS 시스템 환경 설정을 동적으로 처리해야 합니다.
  - 특히 영수증 출력 레이아웃 실시간 가공, `@dnd-kit` 기반의 POS 판매 화면 단축키(키패드) 실시간 배치, 듀얼 디스플레이 간 결제 대기/홍보 동영상 상태 실시간 동기화 등 높은 복잡도의 UI/UX 및 하드웨어 연동 로직이 요구되었습니다.
- **해결 방법 & 설계 포인트**:
  - **POS 글로벌 시스템 설정 동기화 아키텍처 (`pos-settings`, `customer-display`)**: POS 동작 모드, 결제 단말기 연동 파라미터, 듀얼 고객 디스플레이 연동 상태를 전역 `Zustand` 스토어로 구조화하고, `BroadcastChannel` 및 Web API(EventSource/WebSocket)를 활용해 메인 POS 화면과 서브 고객 디스플레이 간의 상태를 0.1초 이내 실시간 동기화했습니다.
  - **영수증 템플릿 렌더링 & 커스텀 엔진 (`pos-receipt`)**: 로고 크기(`SMALL`~`LARGE`), 텍스트 정렬, 사업자 정보, 바코드/QR코드 매핑, 사용자 지정 문구 등을 실시간 캔버스/DOM 미리보기로 제공하고, `@media print` 및 Canvas 픽셀 변환 기반으로 프린터 해상도 및 종이 규격에 맞게 픽셀 유실 없는 고품질 출력을 지원하는 엔진을 구축했습니다.
  - **Drag & Drop 기반 POS 단축키 커스텀 배치 시스템 (`pos-shortcutkey`)**: `@dnd-kit/core` 및 `@dnd-kit/sortable`을 활용해 매장 매니저가 POS 판매 화면의 카테고리/상품 단축키 그리드 레이아웃을 마우스 드래그로 자유롭게 배치하고, 키패드 색상/폰트/크기를 실시간 커스터마이징할 수 있는 오버레이 인터랙션을 개발했습니다.
  - **디자인 토큰 기반 헤드리스 UI**: Tailwind CSS + Radix UI Primitives + `CVA(class-variance-authority)` 조합을 활용해 POS 시스템 설정 화면 전반의 접근성(a11y)과 일관성을 보장하는 컴포넌트 시스템을 모듈화했습니다.

---

## ⚔️ 3. 심층 꼬리질문 (Deep-Dive Q&A) & 6년 차 모범 답변

| 면접관 심층 꼬리질문                                                                                  | 6년 차 개발자의 답변 핵심 전략                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| :---------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Q. 왜 tRPC를 선택했나요? REST나 GraphQL 대비 장점은 무엇인가요?**                                   | "Next.js 기반 모노레포/풀스택 환경이었기에, GraphQL처럼 CodeGen이나 별도 쿼리 언어 스키마 생태계를 구축하는 오버헤드 없이 **Pure TypeScript 타입 추론만으로 프론트와 백엔드가 100% 실시간 동기화되는 tRPC**가 생산성 및 빌드 타임 안전성 면에서 가장 탁월했기 때문입니다."                                                                                                                                                                                                                                            |
| **Q. Zustand와 unstated-next(Container)를 조합하여 쓴 이유는?**                                       | "모든 피처 상태를 Zustand 전역 스토어에 넣으면 불필요한 의존성 얽힘과 메모리 누수가 발생합니다. **Zustand는 인증/POS 설정 등 앱 전반 데이터에 집중**시키고, 특정 화면의 복잡한 비즈니스 로직과 API 상태는 **`unstated-next` Container로 묶어 해당 View 언마운트 시 자동 GC(가비지 컬렉션)** 되도록 2-Layer로 분리하여 스토어 청정도를 유지했습니다."                                                                                                                                                                  |
| **Q. 대용량 재고 데이터 가상화 적용 시 발생한 트러블슈팅 경험이 있나요?**                             | "가상화(`useVirtualizer`) 적용 중 행(Row)의 높이가 동적으로 변할 때 스크롤 튀김(Jump) 현상이 있었습니다. 이를 해결하기 위해 정확한 `estimateSize`를 지정하고, DOM 요소의 동적 측정(`measureElement`)과 `fetchNextPage` 비동기 로딩 시 스크롤 offset 위치를 보정하는 계산 로직 및 옵저버 패턴을 결합하여 완벽한 실시간 가상화 스크롤을 구현했습니다."                                                                                                                                                                  |
| **Q. FSD(Feature-Sliced Design)를 원본 그대로 쓰지 않고 변형한 이유는?**                              | "정통 FSD의 6단계 레이어(`shared` -> `entities` -> `features` -> `widgets` -> `pages` -> `app`)는 당사 프로젝트 규모 대비 폴더 탐색 피로도가 높았습니다. 실무 효율을 위해 Next.js App Router 생태계에 맞춰 도메인 내부에 `view`(UI) / `container`(로직) / `components` / `types` / `constants`로 단순화하여, **FSD의 핵심인 '단방향 의존성'과 '관심사 분리' 원칙은 엄격히 준수하되 팀 학습 곡선과 탐색 오버헤드를 대폭 낮췄습니다.**"                                                                                 |
| **Q. POS 시스템 설정(영수증 템플릿, 단축키 DnD, 듀얼 디스플레이) 개발 시 복잡했던 점과 해결 방식은?** | "POS 설정은 하드웨어 연동 및 실시간 UX 제어가 핵심이었습니다. **단축키 배치**의 경우 `@dnd-kit`을 활용해 그리드 드래그 앤 드롭 성능과 탭 이동 시의 순서 정렬을 가상화 데이터 구조로 보장했습니다. **듀얼 고객 디스플레이**는 메인 POS와 서브 창 간의 상태를 `BroadcastChannel` 및 `Zustand`로 결합하여 결제 금액/상품 추가 시 0.1초 미만 지연 없이 렌더링되도록 구현했습니다. **영수증 템플릿**은 캔버스 미리보기와 `@media print` 픽셀 비율을 동기화하여 실제 감열지 프린터 인쇄 시 비율이 일치하도록 해결했습니다." |

---

## 🎁 4. 면접 전 최종 체크리스트 (Check List)

1. **아키텍처 다이어그램 트레이닝**:
   - `Next.js App Router` ↔ `tRPC / Zod` ↔ `Zustand / Container` ↔ `View` 간의 데이터 흐름과 단방향 의존성을 화이트보드나 종이에 즉석에서 그릴 수 있도록 복기하세요.
2. **핵심 트러블슈팅 사례 2가지 체화**:
   - **[사례 A - 성능]**: 수만 건 데이터 가상화 & `xlsx-js-style` 클라이언트 스트림 가공 시 브라우저 메모리/렌더링 병목 해결 사례
   - **[사례 B - 도메인]**: POS 결제 예외 상황 시 Alert/Confirm 모달 정책 수립 및 `useOverlay` 선언적 비동기 모달 제어 개선 경험
